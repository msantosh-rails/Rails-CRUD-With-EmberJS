<td>{{employee.fullName}}</td>
<td>{{employee.city}}</td>
<td>{{employee.experience}}</td>

<td class="data">
  {{#if isEditing}}
    {{view App.EditEmployeeView}}
  {{else}}

    {{employee.skills}}
  {{/if}}
  <div class="commands">
    {{#unless isEditing}}
      <a href="#" {{action "showEdit"}}>Edit</a>
      <a href="#" {{action "destroyRecord"}}>Remove</a>
    {{/unless}}
  </div>
</td>
